import { doctors,treatments } from "../../data/clinic";
import { AdminHeader } from "./Dashboard";
export default function Directory({type}){const isDoctors=type==="doctors";const items=isDoctors?doctors:treatments;return <><AdminHeader title={isDoctors?"Doctores":"Tratamientos"} subtitle={isDoctors?"Directorio del equipo y disponibilidad semanal.":"Catálogo clínico y precios orientativos de la demo."}/><div className="admin-content"><section className="panel directory-grid">{items.map(item=>{const Icon=item.icon;return <article key={item.id}><span className="directory-avatar">{isDoctors?item.initials:<Icon/>}</span><div><h3>{item.name}</h3><p>{isDoctors?item.role:item.summary}</p><small>{isDoctors?item.days:`${item.duration} · ${item.price}`}</small></div></article>})}</section></div></>}
