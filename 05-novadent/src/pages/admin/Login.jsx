import { useState } from "react";
import { Link,useNavigate } from "react-router-dom";
import { ArrowLeft,Eye,EyeOff,LockKeyhole,ShieldCheck } from "lucide-react";
import Brand from "../../components/Brand";
export default function Login(){const [show,setShow]=useState(false);const [error,setError]=useState("");const [values,setValues]=useState({email:"admin@novadent.demo",password:"demo123"});const navigate=useNavigate();const submit=e=>{e.preventDefault();if(values.email==="admin@novadent.demo"&&values.password==="demo123"){localStorage.setItem("novadent_admin_session","demo");navigate("/admin")}else setError("Las credenciales no coinciden con la cuenta demo.")};return <main className="login-page"><section className="login-brand"><Brand/><div><span>GESTIÓN CLÍNICA</span><h1>Todo el consultorio,<br/>en una sola vista.</h1><p>Organiza citas, pacientes y equipo desde un panel diseñado para el trabajo diario.</p></div><small>Proyecto demostrativo · Sin autenticación real</small></section><section className="login-form-wrap"><Link to="/" className="login-back"><ArrowLeft/> Volver al sitio</Link><form className="login-card" onSubmit={submit}><span className="login-icon"><LockKeyhole/></span><h2>Bienvenida de nuevo</h2><p>Accede al panel administrativo de NovaDent.</p><label>Correo electrónico<input type="email" value={values.email} onChange={e=>{setValues({...values,email:e.target.value});setError("")}}/></label><label>Contraseña<div className="password-field"><input type={show?"text":"password"} value={values.password} onChange={e=>{setValues({...values,password:e.target.value});setError("")}}/><button type="button" onClick={()=>setShow(!show)} aria-label={show?"Ocultar contraseña":"Mostrar contraseña"}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="login-submit">Iniciar sesión</button><div className="demo-credentials"><ShieldCheck/><div><strong>Credenciales de demostración</strong><span>admin@novadent.demo</span><span>demo123</span></div></div></form></section></main>}
