import{useEffect,useRef,useState}from"react";import{CheckCircle2,X}from"lucide-react";export default function ContactModal({property,onClose}){const[done,setDone]=useState(false),[error,setError]=useState(""),close=useRef();useEffect(()=>{close.current?.focus();const key=e=>e.key==="Escape"&&onClose();document.addEventListener("keydown",key);return()=>document.removeEventListener("keydown",key)},[onClose]);const submit=e=>{e.preventDefault();const data=new FormData(e.currentTarget);if(!data.get("name")||!/^\S+@\S+\.\S+$/.test(data.get("email"))){setError("Escribe tu nombre y un correo válido.");return}setDone(true)};return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="modal" role="dialog" aria-modal="true" aria-labelledby="contact-title"><button ref={close} className="modal-close" onClick={onClose} aria-label="Cerrar contacto"><X/></button>{done?<div className="contact-success"><CheckCircle2/><span>SOLICITUD RECIBIDA</span><h2 id="contact-title">Pronto conversaríamos contigo.</h2><p>Esta es una demostración: no enviamos tus datos ni realizaremos una llamada real.</p><button onClick={onClose}>Entendido</button></div>:<><span className="kicker">AGENDA UNA VISITA</span><h2 id="contact-title">Conoce {property.name}</h2><p>Déjanos tus datos para simular una solicitud de visita.</p><form onSubmit={submit}><label>Nombre<input name="name" autoComplete="name"/></label><label>Correo<input name="email" type="email" autoComplete="email"/></label><label>Teléfono<input name="phone" inputMode="tel"/></label><label>Mensaje<textarea name="message" defaultValue={`Me interesa conocer ${property.name}.`}/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="primary">Enviar solicitud</button></form><small>Proyecto demostrativo. La información permanece en este formulario.</small></>}</section></div>}
