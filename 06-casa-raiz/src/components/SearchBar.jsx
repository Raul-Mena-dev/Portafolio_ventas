import{useState}from"react";import{useNavigate}from"react-router-dom";import{ArrowRight,MapPin}from"lucide-react";import{locations}from"../data/properties";export default function SearchBar(){const navigate=useNavigate();const[q,setQ]=useState("");const[type,setType]=useState("");const submit=e=>{e.preventDefault();const params=new URLSearchParams();if(q)params.set("location",q);if(type)params.set("type",type);navigate(`/properties?${params}`)};return <form className="home-search" onSubmit={submit}><label><span>¿DÓNDE QUIERES VIVIR?</span><div><MapPin/><select aria-label="Ubicación" value={q} onChange={e=>setQ(e.target.value)}><option value="">Todo México</option>{locations.map(x=><option key={x}>{x}</option>)}</select></div></label><label><span>TIPO DE PROPIEDAD</span><select aria-label="Tipo de propiedad" value={type} onChange={e=>setType(e.target.value)}><option value="">Todos los espacios</option>{["Casa","Departamento","Terreno","Local"].map(x=><option key={x}>{x}</option>)}</select></label><button>Explorar propiedades <ArrowRight/></button></form>}
