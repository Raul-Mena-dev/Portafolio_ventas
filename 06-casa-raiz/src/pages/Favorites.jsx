import{Link}from"react-router-dom";import{ArrowRight,Heart}from"lucide-react";import PropertyCard from"../components/PropertyCard";import{properties}from"../data/properties";import{useFavorites}from"../hooks/FavoritesContext";export default function Favorites(){const{favorites}=useFavorites(),saved=properties.filter(p=>favorites.includes(p.id));return <><section className="favorites-hero shell"><span className="kicker">TU COLECCIÓN</span><h1>Lugares que quieres<br/><em>volver a mirar.</em></h1><p>{saved.length?`${saved.length} ${saved.length===1?"propiedad guardada":"propiedades guardadas"}`:"Aquí aparecerán los espacios que guardes durante tu búsqueda."}</p></section><section className="shell favorites-body">{saved.length?<div className="catalog-grid">{saved.map(p=><PropertyCard property={p} key={p.id}/>)}</div>:<div className="empty-favorites"><Heart/><h2>Tu colección está esperando.</h2><p>Explora el catálogo y usa el corazón para guardar los lugares que te interesen.</p><Link to="/properties">Descubrir propiedades <ArrowRight/></Link></div>}</section></>}
