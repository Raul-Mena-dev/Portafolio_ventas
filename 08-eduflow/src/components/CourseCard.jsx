import{Link}from"react-router-dom";import{ArrowUpRight,Clock3,Star}from"lucide-react";import CourseVisual from"./CourseVisual";export default function CourseCard({course,compact=false}){return <article className={`course-card ${compact?"compact":""}`}><Link className="course-card-image" to={`/courses/${course.id}`}><CourseVisual course={course}/><span>{course.category}</span></Link><div className="course-card-copy"><span>{course.level}</span><h3><Link to={`/courses/${course.id}`}>{course.title}</Link></h3><p>con {course.teacher}</p><footer><span><Clock3/>{course.duration}</span><span><Star/> {course.rating}</span><Link to={`/courses/${course.id}`} aria-label={`Ver ${course.title}`}><ArrowUpRight/></Link></footer></div></article>}
