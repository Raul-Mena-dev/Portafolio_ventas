import{useMemo,useState}from"react";import{useSearchParams}from"react-router-dom";import{Search}from"lucide-react";import CourseCard from"../components/CourseCard";import{courses}from"../data/courses";export default function Courses(){const[params,setParams]=useSearchParams(),[query,setQuery]=useState("");const category=params.get("category")||"Todos";const visible=useMemo(()=>courses.filter(c=>(category==="Todos"||c.category===category)&&`${c.title} ${c.teacher} ${c.category}`.toLowerCase().includes(query.toLowerCase())),[category,query]);return <><section className="courses-head"><span className="eyebrow">CATÁLOGO DE CURSOS</span><h1>¿Qué quieres<br/><em>aprender hoy?</em></h1><label><Search/><input aria-label="Buscar cursos" placeholder="Busca por curso, tema o profesor" value={query} onChange={e=>setQuery(e.target.value)}/></label></section><section className="catalog"><div className="category-tabs">{["Todos","Desarrollo","Diseño","3D","Negocios","Creatividad"].map(x=><button className={category===x?"active":""} key={x} onClick={()=>x==="Todos"?setParams({}):setParams({category:x})}>{x}</button>)}</div><div className="catalog-meta"><span>{visible.length} cursos encontrados</span><select aria-label="Ordenar cursos"><option>Más populares</option><option>Mejor valorados</option><option>Más recientes</option></select></div>{visible.length?<div className="course-grid catalog-grid">{visible.map(c=><CourseCard course={c} key={c.id}/>)}</div>:<div className="empty"><Search/><h2>No encontramos ese curso.</h2><p>Prueba con otro término o cambia la categoría.</p><button onClick={()=>{setQuery("");setParams({})}}>Ver todos</button></div>}</section></>}
