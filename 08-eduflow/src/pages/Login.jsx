import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{ArrowLeft,Eye,EyeOff,GraduationCap}from"lucide-react";import Brand from"../components/Brand";export default function Login(){const[show,setShow]=useState(false),[error,setError]=useState(""),[values,setValues]=useState({email:"carlos@eduflow.demo",password:"demo123"});const navigate=useNavigate();const submit=e=>{e.preventDefault();if(values.email==="carlos@eduflow.demo"&&values.password==="demo123"){localStorage.setItem("eduflow_session","demo");navigate("/dashboard")}else setError("Usa las credenciales de demostración indicadas.")};return <main className="login-page"><section className="login-aside"><Brand/><div><span>APRENDER CAMBIA TODO</span><h1>Continúa donde<br/>lo dejaste.</h1><p>Tus cursos, progreso y próximas clases están listos para ti.</p></div><div className="login-quote">“La constancia convierte la curiosidad en una habilidad.”<small>— EQUIPO EDUFLOW</small></div></section><section className="login-main"><Link to="/"><ArrowLeft/> Volver al inicio</Link><form onSubmit={submit}><span className="login-icon"><GraduationCap/></span><h2>Hola de nuevo</h2><p>Ingresa a tu espacio de aprendizaje.</p><label>Correo electrónico<input type="email" value={values.email} onChange={e=>{setValues({...values,email:e.target.value});setError("")}}/></label><label>Contraseña<div className="password"><input type={show?"text":"password"} value={values.password} onChange={e=>{setValues({...values,password:e.target.value});setError("")}}/><button type="button" onClick={()=>setShow(!show)} aria-label={show?"Ocultar contraseña":"Mostrar contraseña"}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="login-submit">Iniciar sesión</button><div className="credentials"><strong>Cuenta demostrativa</strong><code>carlos@eduflow.demo</code><code>demo123</code></div></form></section></main>}
