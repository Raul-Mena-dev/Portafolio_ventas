import{Link}from"react-router-dom";import ServiceVisual from"./ServiceVisual";
export default function ServiceCard({service,compact=false}){const Icon=service.icon;return <article className={`service-card ${compact?"compact":""}`}><ServiceVisual position={service.pos} label={`Servicio de ${service.name}`}/><div className="service-card-body"><div className="service-icon"><Icon size={20}/></div><p className="eyebrow">{service.from}</p><h3>{service.name}</h3><p>{service.summary}</p>{!compact&&<ul>{service.includes.map(x=><li key={x}>{x}</li>)}</ul>}<Link to={`/quote?service=${service.id}`}>Solicitar cotización <span>→</span></Link></div></article>}
