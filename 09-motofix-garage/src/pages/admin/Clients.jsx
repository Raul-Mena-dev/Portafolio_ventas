import{Search,Phone}from"lucide-react";import{useState}from"react";import{useOrders}from"../../hooks/OrdersContext";
export default function Clients(){const{orders}=useOrders();const[q,setQ]=useState("");const rows=orders.filter(o=>`${o.client} ${o.phone}`.toLowerCase().includes(q.toLowerCase()));return <Directory title="Clientes" eyebrow="DIRECTORIO" description="Datos de contacto y última visita registrada." q={q} setQ={setQ} placeholder="Buscar cliente o teléfono"><div className="directory-grid">{rows.map(o=><article key={o.order}><span className="directory-avatar">{o.client.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><h3>{o.client}</h3><a href={`tel:${o.phone}`}><Phone/> {o.phone}</a><p>{o.vehicle}</p><small>Última orden · {o.order}</small></div></article>)}</div></Directory>}
export function Directory({title,eyebrow,description,q,setQ,placeholder,children}){return <div className="admin-page"><div className="admin-title"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div></div><div className="table-tools directory-tools"><label><Search/><input placeholder={placeholder} value={q} onChange={e=>setQ(e.target.value)}/></label></div>{children}</div>}
