import{useMemo,useState}from"react";import{RotateCcw,Search}from"lucide-react";import{statuses,money}from"../../data/garage";import{useOrders}from"../../hooks/OrdersContext";
export default function Orders(){const{orders,updateStatus,reset}=useOrders();const[q,setQ]=useState("");const[filter,setFilter]=useState("Todos");const visible=useMemo(()=>orders.filter(o=>(filter==="Todos"||o.status===filter)&&`${o.order} ${o.vehicle} ${o.client}`.toLowerCase().includes(q.toLowerCase())),[orders,q,filter]);return <div className="admin-page"><div className="admin-title"><div><p className="eyebrow">OPERACIÓN</p><h1>Órdenes de servicio</h1><p>Actualiza una etapa y el cliente la verá en el seguimiento público.</p></div><button className="button button-outline" onClick={()=>confirm("¿Restablecer las órdenes de demostración?")&&reset()}><RotateCcw/> Restablecer demo</button></div><div className="table-tools"><label><Search/><input placeholder="Buscar orden, vehículo o cliente" value={q} onChange={e=>setQ(e.target.value)}/></label><select value={filter} onChange={e=>setFilter(e.target.value)}><option>Todos</option>{statuses.map(s=><option key={s}>{s}</option>)}</select><span>{visible.length} resultados</span></div><div className="table-wrap"><table><thead><tr><th>Orden</th><th>Vehículo</th><th>Cliente</th><th>Servicio</th><th>Estado</th><th>Fecha</th><th>Estimado</th></tr></thead><tbody>{visible.map(o=><tr key={o.order}><td><b>{o.order}</b><small>{o.plate}</small></td><td>{o.vehicle}</td><td>{o.client}<small>{o.phone}</small></td><td>{o.service}</td><td><select className="status-select" value={o.status} onChange={e=>updateStatus(o.order,e.target.value)} aria-label={`Estado de ${o.order}`}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td>{o.date}</td><td>{o.estimate?money(o.estimate):"—"}</td></tr>)}</tbody></table>{!visible.length&&<div className="table-empty">No hay órdenes que coincidan con la búsqueda.</div>}</div></div>}
