import{useState}from"react";import{CarFront}from"lucide-react";import{useOrders}from"../../hooks/OrdersContext";import{Directory}from"./Clients";
export default function Vehicles(){const{orders}=useOrders();const[q,setQ]=useState("");const rows=orders.filter(o=>`${o.vehicle} ${o.plate}`.toLowerCase().includes(q.toLowerCase()));return <Directory title="Vehículos" eyebrow="PARQUE VEHICULAR" description="Unidades asociadas a las órdenes activas." q={q} setQ={setQ} placeholder="Buscar vehículo o placas"><div className="directory-grid vehicles">{rows.map(o=><article key={o.order}><span className="directory-avatar"><CarFront/></span><div><h3>{o.vehicle}</h3><b>{o.plate}</b><p>{o.client}</p><small>{o.service} · {o.status}</small></div></article>)}</div></Directory>}
