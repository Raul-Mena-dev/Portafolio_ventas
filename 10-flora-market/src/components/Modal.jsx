import{useEffect}from"react";import{X}from"lucide-react";export default function Modal({open,onClose,children}){useEffect(()=>{if(!open)return;const f=e=>e.key==="Escape"&&onClose();document.addEventListener("keydown",f);return()=>document.removeEventListener("keydown",f)},[open,onClose]);return open?<div className="modal-layer" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal" role="dialog" aria-modal="true" aria-label="Aviso de demostración"><button aria-label="Cerrar aviso" className="modal-x" onClick={onClose}><X/></button>{children}</div></div>:null}

