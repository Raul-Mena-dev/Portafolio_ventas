import{useEffect,useState}from"react";export default function ProductArt({tile=0,label,className=""}){const[failed,setFailed]=useState(false);useEffect(()=>{const i=new Image();i.onload=()=>setFailed(false);i.onerror=()=>setFailed(true);i.src="/images/product-grid.png"},[]);const x=(tile%4)*33.333,y=Math.floor(tile/4)*33.333;return failed?<div className={`product-art fallback ${className}`} role="img" aria-label={label}>flora</div>:<div className={`product-art ${className}`} role="img" aria-label={label} style={{backgroundPosition:`${x}% ${y}%`}}/>}
