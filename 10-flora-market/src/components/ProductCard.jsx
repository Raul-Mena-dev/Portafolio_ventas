import{Link}from"react-router-dom";import{Plus}from"lucide-react";import ProductArt from"./ProductArt";import{money}from"../data/products";import{useCart}from"../hooks/CartContext";
export default function ProductCard({product}){const{add}=useCart();return <article className="product-card"><Link to={`/products/${product.id}`}><div className="art-wrap">{product.new&&<span className="badge">NUEVO</span>}<ProductArt tile={product.tile} label={product.name}/></div><p>{product.category}</p><h3>{product.name}</h3><span className="price">{money(product.price)}</span></Link><button onClick={()=>add(product.id)} aria-label={`Agregar ${product.name} a la canasta`}><Plus/> Agregar</button></article>}
