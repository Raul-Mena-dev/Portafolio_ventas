import{useMemo,useState}from"react";import{useSearchParams}from"react-router-dom";import{Leaf,Search,SlidersHorizontal,X}from"lucide-react";import ProductCard from"../components/ProductCard";import{categories,products}from"../data/products";
export default function Products(){const[params,setParams]=useSearchParams();const start=params.get("category")||"Todos";const category=categories.includes(start)?start:"Todos";const[q,setQ]=useState("");const[sort,setSort]=useState("featured");const shown=useMemo(()=>products.filter(p=>(category==="Todos"||p.category===category)&&`${p.name} ${p.description}`.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==="low"?a.price-b.price:sort==="high"?b.price-a.price:a.id.localeCompare(b.id)),[category,q,sort]);const choose=c=>{setParams(c==="Todos"?{}:{category:c})};return <><section className="shop-head"><small>LA TIENDA</small><h1>Cosas bonitas.<br/><em>Hechas con intención.</em></h1><p>Una colección en movimiento de objetos, plantas y aromas para acompañar tus días.</p></section><section className="shop-layout"><aside><div className="filter-title"><SlidersHorizontal/> FILTRAR</div>{categories.map(c=><button className={category===c?"active":""} key={c} onClick={()=>choose(c)}>{c}<span>{c==="Todos"?products.length:products.filter(p=>p.category===c).length}</span></button>)}</aside><div className="catalog"><div className="catalog-tools"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar en la colección"/><button onClick={()=>setQ("")} aria-label="Limpiar búsqueda">{q&&<X/>}</button></label><span>{shown.length} PIEZAS</span><select value={sort} onChange={e=>setSort(e.target.value)} aria-label="Ordenar productos"><option value="featured">Destacados</option><option value="low">Precio: menor a mayor</option><option value="high">Precio: mayor a menor</option></select></div>{shown.length?<div className="catalog-grid">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="no-results"><Leaf/><h2>No encontramos esa pieza.</h2><p>Prueba otra palabra o explora todas las categorías.</p><button onClick={()=>{setQ("");choose("Todos")}}>Ver toda la colección</button></div>}</div></section></>}

